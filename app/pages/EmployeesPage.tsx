import type {Company} from "../lib/types";
export function EmployeesPage({rows,company}:{rows:string[];company:Company}){return <><div className="toolbar"><span className="count">{rows.length} сотрудников в доступном контуре</span></div><div className="report-grid">{rows.map(x=>{const [name,title]=x.split(" · ");return <div className="card report" key={x}><span className="company-logo">{name.split(" ").map(v=>v[0]).slice(0,2).join("")}</span><div><strong>{name}</strong><small>{title} · {company.toUpperCase()}</small></div><span className="badge">Активен</span></div>})}</div></>}
